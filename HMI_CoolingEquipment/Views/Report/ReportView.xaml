<UserControl x:Class="HMI_CoolingEquipment.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HMI_CoolingEquipment.Views"
             xmlns:cmd="http://www.galasoft.ch/mvvmlight"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:liveCharts="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">


    <UserControl.Resources>
        <Style x:Key="ValueTextBlockStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Padding" Value="4 2" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.23" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="KeyTextBlockStyle" TargetType="{x:Type TextBlock}"
           BasedOn="{StaticResource ValueTextBlockStyle}">
            <Setter Property="FontWeight" Value="Black" />
        </Style>

        <Style x:Key="TitleTextBlockStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource ValueTextBlockStyle}">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontSize" Value="20" />
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0.2*"/>
            </Grid.ColumnDefinitions>
            <GroupBox Grid.Row="0" Header="LoadPort Data">
                <liveCharts:CartesianChart Series="{Binding ViewModel.Series}">
                    <liveCharts:CartesianChart.AxisX>
                        <liveCharts:Axis Title="LoadPorts" Labels="{Binding ViewModel.Labels}" ShowLabels="False">
                        </liveCharts:Axis>
                    </liveCharts:CartesianChart.AxisX>
                    <liveCharts:CartesianChart.AxisY>
                        <liveCharts:Axis Title="Sold Apps" LabelFormatter="{Binding ViewModel.OEEHourlyUphLabelFormatter}"></liveCharts:Axis>
                    </liveCharts:CartesianChart.AxisY>
                </liveCharts:CartesianChart>
            </GroupBox>
            <UniformGrid Grid.Column="1" Rows="2">
                <GroupBox Header="Tray LoadPort Utilization">
                    <liveCharts:Gauge From="0" To="100" LabelsVisibility="Hidden" GaugeBackground="#FF002E36" GaugeActiveFill="#FF00E4c5"  Value="10"/>
                </GroupBox>
                <GroupBox Header="Tube LoadPort Utilization">
                    <liveCharts:Gauge From="0" To="100" LabelsVisibility="Hidden" GaugeBackground="#FF002E36" GaugeActiveFill="#FF00E4c5"  Value="50"/>
                </GroupBox>
            </UniformGrid>
        </Grid>
    </Grid>
</UserControl>

<Window x:Class="HMI_CoolingEquipment.Views.SecsGemCusEventWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SecsGemCusEventWindow" Height="450" Width="800">

    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}" x:Key="WrapText">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="FontSize" Value="15"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>
            <GroupBox Grid.Row="0" Header="SecsGem Custom Event List">
                <DataGrid AutoGenerateColumns="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" x:Name="dataGrid1" HorizontalGridLinesBrush="Black" GridLinesVisibility="All" CanUserAddRows="False" ItemsSource="{Binding ViewModel.SecsGemCusEventList}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Event ID" Binding="{Binding EventID}" Width="*" ElementStyle="{StaticResource WrapText}"/>
                        <DataGridTextColumn Header="Event Name" Binding="{Binding EventName}" Width="*" ElementStyle="{StaticResource WrapText}"/>
                        <DataGridTextColumn Header="Event Group" Binding="{Binding EventGroup}" Width="*" ElementStyle="{StaticResource WrapText}"/>
                        <DataGridTextColumn Header="Event Text" Binding="{Binding EventText}" Width="*" ElementStyle="{StaticResource WrapText}"/>
                        <DataGridTextColumn Header="Event Enable" Binding="{Binding EventEnable}" Width="*" ElementStyle="{StaticResource WrapText}"/>
                        <DataGridTemplateColumn Width="200">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Select" Command="{Binding SelectEventCommand}" CommandParameter="{Binding }"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
    </Grid>
</Window>

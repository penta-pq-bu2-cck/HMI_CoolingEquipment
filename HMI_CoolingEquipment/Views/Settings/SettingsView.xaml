<UserControl x:Class="HMI_CoolingEquipment.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HMI_CoolingEquipment.Views" xmlns:mahApps="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <GroupBox Grid.Row="0" Header="Configuration">
            <StackPanel Orientation="Horizontal">
                <GroupBox Header="Alarm">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Import Alarm From HMI List" Style="{StaticResource KeyTextBlockStyle}" Margin="0 0 20 0" FontSize="15"/>
                        <Button Style="{StaticResource ButtonBaseStyle}" Command="{Binding ViewModel.ImportAlarmCommand}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="0" Source="pack://application:,,,/HMI_CoolingEquipment;component/Images/Icons/Import.png" Width="40" />
                                <TextBlock Grid.Column="1" Style="{StaticResource ValueTextBlockStyle}" Text="Import"/>
                            </Grid>
                        </Button>
                    </StackPanel>
                </GroupBox>
                <GroupBox Header="Setting" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Import Settings From HMI List" Style="{StaticResource KeyTextBlockStyle}" Margin="0 0 20 0" FontSize="15"/>
                        <Button Style="{StaticResource ButtonBaseStyle}" Command="{Binding ViewModel.ImportSettingsCommand}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="0" Source="pack://application:,,,/HMI_CoolingEquipment;component/Images/Icons/Import.png" Width="40" />
                                <TextBlock Grid.Column="1" Style="{StaticResource ValueTextBlockStyle}" Text="Import"/>
                            </Grid>
                        </Button>
                    </StackPanel>
                </GroupBox>
                <GroupBox Header="LoadPort">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Import LoadPort Configuration From HMI List" Style="{StaticResource KeyTextBlockStyle}" Margin="0 0 20 0" FontSize="15"/>
                        <Button Style="{StaticResource ButtonBaseStyle}" Command="{Binding ViewModel.ImportLoadPortConfigurationCommand}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="0" Source="pack://application:,,,/HMI_CoolingEquipment;component/Images/Icons/Import.png" Width="40" />
                                <TextBlock Grid.Column="1" Style="{StaticResource ValueTextBlockStyle}" Text="Import"/>
                            </Grid>
                        </Button>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </GroupBox>
        <GroupBox Grid.Row="1" Header="Settings">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ScrollViewer Grid.Row="0" Grid.Column="0">
                    <StackPanel Orientation="Vertical">
                        <ItemsControl ItemsSource="{Binding ViewModel.Settings, Converter={StaticResource SettingsSplitContentConverter}, ConverterParameter=Numeric}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <GroupBox Header="{Binding Items}" VerticalAlignment="Stretch" Height="70">
                                        <mahApps:NumericUpDown Value="{Binding Value}" />
                                    </GroupBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
                <ScrollViewer Grid.Row="0" Grid.Column="2">
                    <StackPanel Orientation="Vertical">
                        <ItemsControl ItemsSource="{Binding ViewModel.Settings, Converter={StaticResource SettingsSplitContentConverter}, ConverterParameter=Toggle}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <GroupBox VerticalAlignment="Stretch" Height="70">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock x:Name="ToggleItem" Text="{Binding Items}" Style="{StaticResource KeyTextBlockStyle}" FontSize="15"/>
                                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                                <TextBlock Text="Off" Style="{StaticResource RightTextBlock}"/>
                                                <mahApps:ToggleSwitch IsChecked="{Binding Value}" VerticalAlignment="Center"/>
                                                <TextBlock Text="On" Style="{StaticResource LeftTextBlock}" />
                                            </StackPanel>
                                        </Grid>
                                    </GroupBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
                <ScrollViewer Grid.Row="0" Grid.Column="1">
                    <StackPanel Orientation="Vertical">
                        <ItemsControl ItemsSource="{Binding ViewModel.Settings, Converter={StaticResource SettingsSplitContentConverter}, ConverterParameter=ComboBox}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <GroupBox VerticalAlignment="Stretch" Height="80" Header="{Binding Items}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <!--<TextBlock Text="{Binding Items}" Style="{StaticResource KeyTextBlockStyle}" FontSize="15"/>-->
                                            <ComboBox ItemsSource="{Binding ComboBoxValues}" HorizontalAlignment="Left" MinWidth="200" HorizontalContentAlignment="Center" SelectedIndex="{Binding Value}"/>
                                        </Grid>
                                    </GroupBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
                <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Content="Save" Command="{Binding ViewModel.SaveButtonCommand}"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
